请代我把 BioData Agent 的在线 MCP 服务接入我的 AI 客户端（Claude Code / Kimi Code / Codex 等支持 MCP 的助手），全程代我完成，装好后直接验收。

【我的接入配置（已生成，直接照用）】

```json
{
  "mcpServers": {
    "biodata": {
      "url": "__BIODATA_MCP_URL__",
      "headers": { "Authorization": "Bearer __BIODATA_MCP_TOKEN__" }
    }
  }
}
```

【步骤】
1. 找到我这个客户端登记 MCP 服务器的方式（配置文件或 CLI 命令）。不同客户端字段名略有差异：核心是「http 型（streamable HTTP）服务器 + URL + Authorization 请求头」三要素；拿不准字段名就查该客户端自己的 MCP 文档或 `--help`，不要臆造。服务器名统一用 `biodata`；先查重，同名且 URL 一致就跳过。
2. 把上面的配置登记进去，然后按客户端要求重启或重载使其生效。
3. 验收：通过 MCP 调一次 `biodata_status`，要求 `ok=true`、`corpus_total>0`，并列出你实际看到的工具清单。不要用 shell 或网页代替 MCP 调用。

【这个服务能做什么】
- 公开单细胞/空间组学数据集目录的中文检索、浏览、详情、引文导出、任务包预览等，在线共 17 个工具。
- 纯确定性服务：不消耗任何 LLM 额度。`use_llm=true`、`rerank="llm"` 这类显式 LLM 参数会被显式拒绝（报 `online_llm_disabled`），去掉后重试即可。
- 我的语料读写都落在我的账户空间里。
- 「下载数据集到本机」（provision_dataset）和「本地资产核验」（verify_local_assets）是本机文件系统能力，在线形态不提供——tools/list 里本来就看不到它们，属预期。

【安全要求】
- 上面的令牌等同于我的账户访问凭证：不要打印到日志、不要写进会提交到 git 的文件、不要发到任何公开渠道；只写进我客户端的 MCP 配置。
- 如果泄露或不用了，告诉我到 BioData 网页版「帮助 → 接入 AI 助手」里吊销。

【收尾报告】
告诉我：登记到了哪个客户端、用的配置文件路径或命令、重启/重载方式、验收时 `biodata_status` 的 `ok` / `corpus_total` / 工具数。任何一步没做或失败，如实说卡在哪、需要我做什么，不要假装完成。
